[project]
name = "plantify"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "flask>=3.1.2",
    "pillow>=12.0.0",
    "torch>=2.9.1",
    "transformers>=4.57.3",
]
